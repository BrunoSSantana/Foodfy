{% extends "layout.njk" %}

{% block head %}
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title>Receitas</title>
{% endblock %}

{% block content %}
    <section>
        <div class="mostaccessed">
                <div class="pratos">                    
                    {% for data in datas %}
                        <div class="prato" id="{{data.id}}">
                            <img src="{{data.image}}" alt="{{data.title}}">
                            <h4 class="nome-do-prato" >{{data.title}}</h4>
                            <p class="chef">por {{data.author}}</p>
                        </div>
                    {% endfor %}

                    {% for data in datas %}
                        <div class="prato" id="{{data.id}}">
                            <img src="{{data.image}}" alt="{{data.title}}">
                            <h4 class="nome-do-prato" >{{data.title}}</h4>
                            <p class="chef">por {{data.author}}</p>
                        </div>
                    {% endfor %}

                </div>
            
        </div>
    </section>
{% endblock %}
