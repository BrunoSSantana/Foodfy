{% extends "layout.njk" %}

{% block head %}
    <title>{{recipe.title}}</title>
{% endblock %}

{% block content %}

    <div class="banner">
        <img src="{{recipe.image}}" alt="{{recipe.title}}">
        <h4 class="nome-do-prato" >{{recipe.title}}</h4>
        <p class="chef">por {{recipe.author}}</p>
        <div class="description">

            <div class="partial">
                <div class="head">
                    <h5>Ingredientes</h5><button id="ingredientes" class="button">ocultar</button>
                </div>

                <a id="ingredientes"><p><ul>
                    {% for item in recipe.ingredients %}
                        <li>{{item}}</li>
                    {% endfor %}
                </ul></a>
            </div>

            <div class="partial">
                <div class="head">
                    <h5>Modo de preparo</h5><button id="mod" class="button">ocultar</button>
                </div>

                <a id="mod"><p>{{recipe.preparation}}</p></a>
            </div>

            <div class="partial">
                <div class="head">
                    <h5>Informações Adicinais</h5><button id="infos" class="button">ocultar</button>
                </div>

                <a id="infos"><p>{{recipe.information}}</p></a>
            </div>

        </div>
    </div>
{% endblock %}
